package game;

/** SquareState - an enum of the different states to be tracked for each square
 * of a Board.
 * 
 * @author Nathan Jobe
 * @version %I%, %G%
 */
public enum SquareState {
	UNKNOWN, KNOWN, FLAGGED, DEAD
}
